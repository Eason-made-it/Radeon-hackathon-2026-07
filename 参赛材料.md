# NodeFlow 参赛材料

> AMD AI DevMaster Hackathon Track 1 参赛配套材料
> 作品：NodeFlow — 无限画布 AI 风格化工作站

---

## 一、演示视频要求

### 1.1 建议规格

| 项目 | 建议 |
|------|------|
| 时长 | 3–5 分钟（主体 demo 建议 3 分钟，可另附 60–90 秒精华版用于快速评审） |
| 分辨率 | 1080p（1920×1080），横屏 |
| 帧率 | 30fps |
| 画面要素 | 操作录屏 + 正面镜头（可选）+ 后期字幕/卖点标注 |
| 音频 | 中文普通话旁白，清晰、语速适中，配背景音乐音量压低 |

### 1.2 内容脚本框架（分镜建议）

**第 1 镜 · 开场（约 15 秒）**
- 画面：产品 Logo 与「NodeFlow — 无限画布 AI 风格化工作站」标题。
- 旁白：一句话点题——「在 AMD Radeon GPU 上，一边画草图，一边让 AI 帮你完成作品。」
- 目的：让评委 10 秒内知道「这是什么」。

**第 2 镜 · 画布创作（约 40 秒）**
- 画面：进入无限画布，手绘一个简单草图（人物/场景轮廓），然后切换到风格面板。
- 动作：依次选择 Cyberpunk、Watercolor、3D Render 等多种风格，展示同一草图在不同风格下的结果。
- 旁白：突出「不是填文字框，而是直接在画布上画」的差异化交互。
- 目的：让评委看到「画草图 → AI 风格化」的核心链路与画布交互的创作感。

**第 3 镜 · 双模式与参数（约 40 秒）**
- 画面：演示 Fast 快速版秒级出图，再切换到 Expert 专家版，调节步数 / 引导强度 / 种子 / 风格化强度。
- 旁白：说明「Fast 求速度、Expert 求质量」的双模式设计。
- 目的：讲清「速度与质量兼顾」这一卖点。

**第 4 镜 · 多模型与 LoRA（约 40 秒）**
- 画面：打开模型选择面板，在 SDXL-Lightning / FLUX.2-klein / NoobAI XL 之间热切换；打开 LoRA 面板，拖动权重看画面变化。
- 旁白：说明「模型热切换 + LoRA 权重调节」让用户掌控风格。
- 目的：展示工程的架构能力与产品对风格的可控性。

**第 5 镜 · 模型商店（约 20 秒）**
- 画面：浏览模型商店，展示从 HuggingFace/Civitai 浏览与下载模型。
- 旁白：一句话带过，说明可扩展性。
- 目的：体现产品的生态与开放性。

**第 6 镜 · 收尾（约 15 秒）**
- 画面：回到产品界面，叠加「AMD Radeon GPU · ROCm 原生推理」字样。
- 旁白：收束到「不依赖 CUDA，跑在开放 GPU 生态上」的落点。
- 目的：呼应比赛主题，强化差异化。

### 1.3 评委会看什么

- **是否真实跑在 AMD 硬件上**：应在画面中展示 GPU 名称 AMD Radeon PRO W7900、运行时 ROCm、健康检查接口 / GPU 显存状态，证明「原生 ROCm 推理」不是 PPT 话术。
- **是否解决了真实问题**：评委关注「为什么不用 ComfyUI / 普通文生图框」——视频要呈现画布交互带来的低门槛与创作感。
- **工程完成度**：多模型热切换、双模式、LoRA、模型商店这些能力要能「现场点出来」，而不是只出现在架构图里。
- **演示流畅度**：提前预加载模型，避免现场等待下载；每条 demo 操作路径要演练到零失误。

---

## 二、演示旁白台词（60–90 秒精华版）

> 以下一段约 80 秒，可直接作为演示视频旁白或评委评审时的口头陈述。

大家好，这是 NodeFlow——一个跑在 AMD Radeon GPU 上的 AI 风格化工作站。

它不是普通的「文字生成图片」输入框，而是给你一块无限画布。你直接在画布上画草图，选择一种风格，AI 就会把草图重绘成完整的作品。刚才这张手绘的人物线稿，我分别用 Cyberpunk、水彩和 3D 渲染三种风格各生成了一张，同一个灵感，三种完全不同的表达。

NodeFlow 有两种模式。Fast 快速版用 SDXL-Lightning 蒸馏模型，四到八步就出图，几乎是秒级，适合快速预览和反复试稿。Expert 专家版用 FLUX.2-klein、NoobAI XL 这些完整模型，步数、引导强度、随机种子、风格化强度都可以调，适合精修成稿。

更重要的是，风格完全由你掌控。模型可以热切换，LoRA 权重可以实时调节——同一个真实画风的底子，挂上动漫 LoRA，再调一下权重，风格就跟着变。

而这一切，全部跑在 AMD Radeon PRO W7900 上，用的是原生 ROCm 推理，不依赖 NVIDIA CUDA。我们希望告诉大家：AI 图像生成，完全可以跑在开放、国产的 GPU 生态上。

NodeFlow——让画布上的灵感，在 AMD GPU 上变成作品。

---

## 三、使用方法与步骤

### 3.1 打开与选择模式

1. 浏览器打开部署实例的公网地址（由 `rc-tunnel expose --port 8000` 生成的 URL）。
2. 左侧为无限画布（Excalidraw），右侧为控制面板。
3. 顶部工具栏选择输入方式：
   - **Sketch → Art（img2img）**：在画布上手绘草图。
   - **Text → Art（text2img）**：输入文字描述。

### 3.2 选择 Fast / Expert 模式与模型

1. 打开模型选择面板，在「Fast 模型」与「Expert 模型」两个 Tab 间切换。
2. 查看模型卡片上的架构标签、显存占用、许可证与「可商用 / 仅非商用」标记。
3. 点击「加载」切换模型。首次加载会下载并预加载到 GPU；W7900 的 48GB 显存可同时容纳多个底座模型。

### 3.3 选择风格预设

从 8 种风格中选择一种：Cyberpunk（赛博朋克）、Anime（动漫）、Watercolor（水彩）、Oil Painting（油画）、3D Render（3D 渲染）、Pixel Art（像素风）、Concept Art（概念设计）、Minimalist（极简）。

### 3.4 调整参数

- **图生图**：拖动「风格化强度」滑块（0.1–1.0），权衡保留草图原貌与彻底重绘。
- **Expert 模式**：可调整推理步数、引导强度（CFG）、随机种子、负向提示。
- **LoRA**：在 LoRA 面板加载所需 LoRA 并调节权重（0–2.0）；快速版会自动对 LoRA 降权，避免蒸馏模型出噪点。

### 3.5 生成与下载

1. 点击 **Generate**。
   - 图生图：前端将画布导出为 PNG，以 multipart 表单上传到 `/api/generate/img2img`。
   - 文生图：前端将文字以 JSON 提交到 `/api/generate/text2img`。
2. 后端在 AMD Radeon GPU 上完成推理，返回 PNG；结果展示在右侧面板。
3. 点击 **Download PNG** 保存。

### 3.6 演示动线建议（评审现场）

1. 画一个简单草图（10 秒）。
2. 选 Watercolor 风格、强度 0.8，生成一张（展示 img2img 链路）。
3. 切 Fast 快速版再生成一次，展示速度。
4. 切 Expert 版，调高步数再生成，展示质量与参数可控。
5. 打开 LoRA 面板，拖动权重，展示风格变化。
6. 打开模型商店，浏览并（可选）下载一个模型。

---

## 四、优势说明：与同类工具的差异化

### 4.1 对比维度总览

| 维度 | NodeFlow | ComfyUI | Liblib（LiblibAI） | 即梦（Jimeng） |
|------|----------|---------|------------------|----------------|
| 硬件依赖 | AMD Radeon 原生 ROCm，不依赖 CUDA | 主流依赖 NVIDIA CUDA | 云端算力，通常 CUDA 后端 | 云端算力 |
| 创作交互 | 无限画布手绘草图 → AI 风格化 | 节点编辑器（连线工作流） | 文生图框 / 图生图上传 | 文生图框 / 图生图上传 |
| 学习门槛 | 低（画布即创作） | 高（需理解节点与图栈） | 中 | 中（偏模板化） |
| 模式设计 | 双模式（Fast/Expert）：速度与质量兼顾 | 依赖用户自配模型与参数 | 单一生成路径 | 单一生成路径 |
| 风格控制 | 多模型热切换 + LoRA 权重调节 | 强但需手动接线 | 有限（套用在线风格） | 有限（跟随平台模板） |
| 可扩展性 | 内置模型商店（HF/Civitai 聚合） | 生态庞大但装配复杂 | 平台内资源 | 平台内资源 |

### 4.2 与 ComfyUI 的差异

ComfyUI 是功能强大的节点式工作流工具，但上手成本高：需要理解 checkpoint、VAE、LoRA、采样器、CLIP 等概念，并手动搭建节点图。NodeFlow 把「模型怎么接、权重建在哪里」全部封装到后端（ModelManager + 适配器 + 模型注册表），用户只需在画布上画、在面板上选。NodeFlow 以「更低的门槛」换取 ComfyUI 的「深度可控」，适合创作者而非工具极客。

### 4.3 与 Liblib、即梦的差异

Liblib 与即梦是成熟的云端文生图平台，交互是「输入框 / 上传图片 + 选风格」的套餐式生成，风格与模型由平台统一管理，用户很难把「真实底 + 自定义 LoRA」自由组合。NodeFlow 把创作入口放到**无限画布**上，让手绘草图成为构图与创意的起点；同时把模型的**选择权与参数控制权**交给用户，并通过模型商店对接开放的 HuggingFace / Civitai 生态，而不是封闭在平台内。

### 4.4 最核心的差异化落点

1. **AMD / ROCm 原生**：真正在 AMD Radeon PRO W7900 上用 ROCm 推理，不依赖 NVIDIA CUDA，契合本次 AMD 赛事主题，也代表开放 GPU 生态的可行性。
2. **画布交互 = 低门槛 + 创作感**：相比 ComfyUI 的门槛和文生图框的「无创作感」，画布手感更接近真实创作。
3. **双模式兼顾速度与质量**：一套产品覆盖「快速试稿」与「精修成稿」。
4. **多模型 + LoRA 的可控性**：把风格控制权交给用户，且对每个模型/LoRA 标注许可证与商用状态，合规透明。

---

*NodeFlow — 让画布上的灵感，在 AMD GPU 上变成作品。*